# AGENTS.md

## 项目概述

本项目是一个 Rust workspace，当前包含以下 crate：

- `u-sdk`：主要 SDK crate，提供多个可选功能模块。
- `u-sdk-common`：公共基础 crate，被 `u-sdk` 依赖。

每个 crate 位于独立目录，并通过自己的 `Cargo.toml` 描述依赖和配置。执行任务时应聚焦于用户指定的 crate，避免检查或修改无关
crate。需要注意的是，crate 并非完全互相独立：修改 `u-sdk-common` 可能影响 `u-sdk`。

## 操作流程

### 1. 单独操作原则

每次执行任务时，只专注于当前操作的 crate。除非任务涉及 workspace 配置、跨 crate API 或依赖关系，否则不要检查、构建或修改其他
crate。

### 2. 运行 `cargo` 命令时的约束

- 仅允许运行以下 `cargo` 命令：
    - `cargo check`：检查语法和编译错误。
    - `cargo fmt`：格式化代码或检查代码格式。
- 运行 `cargo check` 和 `cargo fmt` 前需要请求用户批准提权。
- 必须先切换到当前操作的 crate 目录，再执行 `cargo` 命令；不得从 workspace 根目录运行。
- 不得运行 `cargo test`、`cargo clippy`、`cargo build`、`cargo update`、`cargo publish` 或其他未明确允许的 `cargo` 命令。

### 3. 子 crate 的 `AGENTS.md`

子 crate 可能包含自己的 `AGENTS.md`。处理某个 crate 前，必须查找并遵循从 workspace 根目录到该 crate 目录路径上的全部
`AGENTS.md`。更深层目录中的规则优先于上层规则。

子 crate 的规则可包括：

- crate 的功能描述；
- 依赖关系；
- 构建和测试要求；
- crate 特有的配置项。

## 工作约束与行为准则

在进行每个步骤时，我们遵循一系列行为准则和约束，以确保操作规范、最小化风险并提高效率。以下是我们遵循的核心准则：

### 1. 分步协作开发模式

**分步执行**：任务按步骤执行，每一步需用户确认。此模式确保每个步骤可控且独立，避免错误传播。

**步骤流程：**

1. **需求分析与设计**：分析需求，设计实现方案，识别风险并提出解决方案。
2. **任务拆分与分步计划**：将任务拆分为独立步骤，每个步骤有明确目标和效果。
3. **执行步骤**：保持最小改动，确保每一步独立完成。
4. **改动说明与验证**：每步完成后提供改动说明，给出验证方法并等待用户确认。
5. **用户反馈**：确认用户反馈后，再继续执行下一步。

**禁止行为：** 一次性完成大量修改，跳过用户确认。

### 2. 简单优先与最小化改动

**目标**：用最少的代码解决问题，不做推测，保持改动的局部性。

- **只做需求中明确要求的功能**，避免无关抽象和复杂性。
- **不对不可能发生的情况做错误处理**。
- 如果代码能够被精简，**将大段代码减少到最小量**，例如将 200 行代码压缩为 50 行。

**测试问题**：每一行改动都应该直接关联到用户的需求，保持代码的简单性。

### 3. 精确修改与最小化副作用

**原则**：修改现有代码时，保持最小化改动，避免重构或修改无关部分。

- **不修改相邻的代码、注释或格式**，不重构未出现问题的代码。
- 如果发现死代码，**提及但不删除**，确保代码状态与原有风格一致。

当你的修改导致代码孤立时：

- 删除修改部分引起的未使用的导入、变量或函数。
- 不要删除原有的死代码，除非被要求。

### 4. Git 与提交约束

- **提交**：建议用户手动提交代码，不自动提交。
- **Git 约束**：禁止修改 Git 历史，提交由用户手动完成。

### 5. 用户的 Git 工作流说明

通常，每次完成一个或多个的修改步骤后（具体根据改动的规模、主题或影响范围），用户会执行一次
commit。这种做法的主要目的是确保每次改动都有清晰的逻辑和完整性。用户的这种逐步提交为 agent 带来以下优势：

- 审查改动内容：用户的提交可以使 agent 方便地查看具体的代码更改，确保改动符合预期。
- 灵活优化：用户在提交前的审查过程中，如果发现细节上的问题或不满意之处，会手动进行优化，纠正 agent 没有注意到的部分。
- 确保每一步正确性：用户提交之前都会手动进行严格校验和测试，确保 agent 的每个改动是完全正确和可行的。

## Changelog 规范

- 对外功能、公开 API、错误行为或缺陷修复发生变化时，应检查当前操作 crate 的 `CHANGELOG.md` 是否需要同步更新，不得修改无关
  crate 的 Changelog。
- Changelog 应沿用目标文件已有的 Keep a Changelog 结构，按变更性质归入 `Added`、`Changed`、`Fixed`、`Removed` 等分类。
- 未发布的变更写入 `[Unreleased]`；只有用户明确版本号和发布日期时，才新增对应的版本章节。
- crate 或模块标记、条目粒度及排列方式应沿用目标 Changelog 的现有格式，不自行引入新的组织结构。
- 中文条目使用中文标点，枚举使用顿号 `、`，说明使用冒号 `：`，每条完整描述以句号 `。` 结尾。
- 中文与英文单词、英文缩写、数字及行内代码之间保留一个半角空格；中文标点前后不添加空格。
- 类型、方法、参数、模块及其他代码标识符使用反引号标记，例如 `Client`、`send_key`。
- 只修改本次任务涉及的条目，不顺带整理或改写历史内容，除非用户明确要求。
- Changelog 不得包含真实密钥、token、账号、资源标识或其他敏感信息。

## 环境与配置约束

### 1. 依赖管理与文件保护

- **禁止修改依赖文件**：不得修改 `Cargo.toml` 等文件。
- **新增依赖**：若必须新增依赖，明确说明并等待用户确认。

### 2. 工程配置保护

- **禁止修改工程配置文件**：不修改 `.vscode/`, `.idea/`, `*.iml` 等配置文件，除非用户明确要求。

### 3. 修改范围限制

- **文件修改范围**：仅修改与任务相关的文件。
- **避免修改输出目录**：不修改 `target/`, `dist/`, `build/` 等目录内容。

## 代码质量要求

### 1. 质量保证

- **确保修改后的代码逻辑正确**，不破坏现有结构，保持一致的代码风格。
- **最小副作用**：避免引入副作用，确保修改不影响其它部分的功能。

### 2. 不确定性处理

- **处理不确定性**：当任务存在不确定性时，明确指出不确定点，说明可能的影响，等待用户确认。

## 最终行为准则

- **先分析，再规划**
- **先确认，再修改**
- **一次只做一步**
- **每步都解释**
- **每步都要求验证**
- **用户确认后再继续**

避免跳步、扩大改动或绕过确认。

## 规则优先级

1. 分步执行 + 用户确认
2. 不修改依赖
3. 不污染环境
4. 最小改动
5. 功能实现
